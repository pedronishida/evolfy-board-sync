{
  "name": "evolfy-board-sync-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test plugins/evolfy-board-sync/scripts/core.test.mjs",
    "check": "npm test && node scripts/check-package.mjs"
  }
}
